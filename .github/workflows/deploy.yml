name: Deploy to College Server (Disabled - Handled via Server Crontab)

# Trigger manually only (Server uses auto-deploy.sh via crontab)
on:
  workflow_dispatch:

jobs:
  build-and-test:
    name: Info
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Deployment is managed automatically by server crontab (auto-deploy.sh)"

